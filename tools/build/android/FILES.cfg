# -*- python -*-
# ex: set syntax=python:

# Remove unused archives:
#FILES = [f for f in FILES if f.get('archive') not in []]

FILES = [f for f in FILES if f.get('filename') not in ['apks/ContentShell.apk',
                                                       'apks/ChromePublic.apk',]]

FILES = FILES + [
  {
    'filename': 'apks/Vivaldi.*.apk',
    'buildtype': ['dev', 'official'],
    'direct_archive': 1,
    'archive': 'Vivaldi.*.apk'
  },
  {
    'filename': 'apks/Vivaldi.*.apk.map',
    'buildtype': ['dev', 'official'],
    'archive': 'Vivaldi.apk.map.zip',
  },
]
