[workspace]
members = [
    "build_scripts",
    "ctap_protocol",
    "platform",
    "remote_auth_protocol",
]
default-members = ["build_scripts"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[workspace.dependencies]
anyhow = "1.0.72"
